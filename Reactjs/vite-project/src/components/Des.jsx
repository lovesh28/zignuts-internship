import React from "react";

const Des = (props) => {
    return ( <
        >
        <
        h1 > { props.title } < /h1> <
        h2 > { props.saal } < /h2> <
        />
    );
};

export default Des;